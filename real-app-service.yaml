apiVersion: v1
kind: Service
metadata:
  name: real-app-service
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: '5000'
spec:
  selector:
    app: real-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
